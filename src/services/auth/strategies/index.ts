import { strategy as JWTStrategy } from './jwt';
import { strategy as FacebookStrategy } from './facebook';
import { strategy as GoogleStrategy } from './google';

export { JWTStrategy, FacebookStrategy, GoogleStrategy };
